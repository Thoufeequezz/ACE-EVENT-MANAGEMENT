<!doctype html>
<html>
<head>
<meta charset="utf-8" />
<style>
  body { font-family: Arial, sans-serif; background:#f9f9f9; margin:0; padding:0; }
  .container { max-width:600px; margin:20px auto; background:#ffffff; border:1px solid #ddd; border-radius:8px; overflow:hidden; }
  .header { background:#0b6ef6; color:#fff; text-align:center; padding:20px; font-size:20px; font-weight:bold; }
  .content { padding:20px; color:#333; }
  .content img { max-width:100%; border-radius:6px; margin-bottom:15px; }
  .footer { font-size:12px; text-align:center; padding:10px; color:#777; border-top:1px solid #eee; }
  .btn { display:inline-block; padding:10px 18px; background:#0b6ef6; color:#fff; border-radius:4px; text-decoration:none; }
</style>
</head>
<body>
<div class="container">
  <div class="header">📢 New Event: <%= title %></div>
  <div class="content">
    <% if (imageUrl) { %>
      <img src="<%= imageUrl %>" alt="Event image" />
    <% } %>
    <p><strong>Fee:</strong> <%= fee %></p>
    <p><strong>Venue:</strong> <%= venue %></p>
    <p><strong>Start Date:</strong> <%= startDate %></p>
    <p><strong>End Date:</strong> <%= endDate %></p>
    <p><strong>Description:</strong><br/> <%= description %></p>
    <p style="text-align:center; margin-top:20px;">
      <a href="<%= eventLink %>" class="btn">View Event</a>
    </p>
  </div>
  <div class="footer">This is an automated newsletter. Please do not reply.</div>
</div>
</body>
</html>
